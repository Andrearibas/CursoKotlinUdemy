package com.example.cursokotlin.fundamentos.controle

fun main() {
    var contador: Int = 1

    //Exemplo de While com quantidade determinada, porém nesse casos recomenda-se utilizar o for
    while (contador <= 10){
        println(contador)
        contador++
    }
}